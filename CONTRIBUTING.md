# Руководство по контрибьютингу

Спасибо за интерес к развитию AVYX Beta! 🎉

## 📋 Содержание

- [Код поведения](#код-поведения)
- [Как внести вклад](#как-внести-вклад)
- [Соглашение о коммитах](#соглашение-о-коммитах)
- [Pull Request процесс](#pull-request-процесс)
- [Стиль кода](#стиль-кода)

## Код поведения

Участвуя в проекте, вы соглашаетесь поддерживать уважительную и инклюзивную среду для всех.

## Как внести вклад

1. **Fork** репозитория
2. Создайте **feature branch** (`git checkout -b feature/amazing-feature`)
3. **Commit** изменения (`git commit -m 'feat: add amazing feature'`)
4. **Push** в branch (`git push origin feature/amazing-feature`)
5. Откройте **Pull Request**

## Соглашение о коммитах

Мы используем [Conventional Commits](https://www.conventionalcommits.org/ru/) для стандартизации сообщений коммитов.

### Формат

```
<type>(<scope>): <description>

[optional body]

[optional footer(s)]
```

### Типы коммитов

| Тип | Описание |
|-----|----------|
| `feat` | Новая функциональность |
| `fix` | Исправление бага |
| `docs` | Изменения в документации |
| `style` | Форматирование, пробелы и т.д. (не влияет на код) |
| `refactor` | Рефакторинг кода |
| `perf` | Улучшение производительности |
| `test` | Добавление или исправление тестов |
| `build` | Изменения в системе сборки |
| `ci` | Изменения в CI конфигурации |
| `chore` | Прочие изменения |
| `revert` | Откат предыдущего коммита |

### Примеры

```bash
# Новая функция
feat(auth): add social login support

# Исправление бага
fix(profile): resolve avatar upload issue

# Документация
docs(readme): update installation instructions

# Рефакторинг
refactor(components): extract Avatar into separate component

# Стиль
style(screens): fix indentation in HomeScreen

# Тесты
test(utils): add unit tests for date formatting
```

### Scope (область)

Опциональная часть, указывающая на область изменений:

- `auth` — авторизация
- `profile` — профиль пользователя
- `tasks` — задания
- `missions` — миссии и квесты
- `ui` — UI компоненты
- `api` — API интеграции
- `config` — конфигурация

### Breaking Changes

Для критических изменений добавьте `!` после типа или `BREAKING CHANGE:` в footer:

```bash
feat(api)!: change response format for tasks endpoint

BREAKING CHANGE: tasks endpoint now returns paginated response
```

## Pull Request процесс

1. Убедитесь, что код проходит линтинг: `npm run lint`
2. Убедитесь, что типы корректны: `npm run type-check`
3. Обновите документацию при необходимости
4. Заполните шаблон PR с описанием изменений
5. Дождитесь code review

### Шаблон PR

```markdown
## Описание
Краткое описание изменений

## Тип изменений
- [ ] Bug fix
- [ ] New feature
- [ ] Breaking change
- [ ] Documentation update

## Чеклист
- [ ] Код соответствует стилю проекта
- [ ] Добавлены тесты (если применимо)
- [ ] Документация обновлена
- [ ] Все тесты проходят
```

## Стиль кода

### TypeScript

- Используйте строгую типизацию
- Избегайте `any`
- Документируйте публичные API с помощью JSDoc

### React

- Функциональные компоненты с хуками
- Именование компонентов в PascalCase
- Props интерфейсы с суффиксом `Props`

### CSS/Tailwind

- Используйте Tailwind классы
- Избегайте inline стилей
- Группируйте классы логически

---

Спасибо за ваш вклад! 🙏
